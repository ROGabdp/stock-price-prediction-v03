# Stock Price Prediction System - Python Dependencies
# Python version: 3.11+

# Web Framework
flask==3.0.0
flask-cors==4.0.0
werkzeug==3.0.1

# Machine Learning Framework
# CPU 版本（預設）:
# tensorflow==2.16.1

# GPU 版本（NVIDIA GPU + CUDA）:
# 如果您有 NVIDIA GPU，請先解除安裝 CPU 版本，再安裝 GPU 版本：
# pip uninstall tensorflow
# pip install tensorflow[and-cuda]==2.16.1
tensorflow==2.16.1

keras-tuner==1.4.6

# Data Processing
pandas==2.1.4
numpy==1.26.2

# Data Validation and Serialization
scikit-learn==1.3.2

# Utilities
python-dateutil==2.8.2
